export enum ETableName {
    CRAWL_STATUS = 'crawl_status',
    BLOCKS = 'blocks',
    TRANSACTIONS = 'transactions',
    ADDRESSES = 'addresses',
}

export enum EAddressType {
    UNDEFINED = 1,
    CONTRACT = 2,
    WALLET = 3,
}
